# Frontend (placeholder)

This folder will contain the Next.js (App Router + TypeScript) frontend for the WhatsApp Automation dashboard.

For Phase 1 we create the skeleton. To scaffold the app later, run:

```bash
cd frontend
npx create-next-app@latest . --ts --app
```
